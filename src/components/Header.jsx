import React from 'react';
import '../App.css';

const Header = () => {
    return (
        <header className="header">
            <h2>📊 Stock Dashboard</h2>
        </header>
    );
};

export default Header;